//
// Copyright Amazon.com Inc. or its affiliates.
// All Rights Reserved.
//
// SPDX-License-Identifier: Apache-2.0
//

import SwiftUI

extension LocalizedStringKey {
    /// en = "Liveness Check"
    static let get_ready_page_title = LocalizedStringKey(
        "amplify_ui_liveness_get_ready_page_title"
    )

    /// en = "You will go through a face verification process to prove that you are a real person. Your screen's brightness will temporarily be set to 100% for highest accuracy."
    static let get_ready_page_description = LocalizedStringKey(
        "amplify_ui_liveness_get_ready_page_description"
    )

    /// en = "Photosensitivity Warning"
    static let get_ready_photosensitivity_title = LocalizedStringKey(
        "amplify_ui_liveness_get_ready_photosensitivity_title"
    )

    /// en = "This check displays colored lights. Use caution if you are photosensitive."
    static let get_ready_photosensitivity_description = LocalizedStringKey(
        "amplify_ui_liveness_get_ready_photosensitivity_description"
    )

    /// en = "Photosensitivity Information"
    static let get_ready_photosensitivity_icon_a11y = LocalizedStringKey(
        "amplify_ui_liveness_get_ready_photosensitivity_icon_a11y"
    )

    /// en = "Photosensitivity warning"
    static let get_ready_photosensitivity_dialog_title = LocalizedStringKey(
        "amplify_ui_liveness_get_ready_photosensitivity_dialog_title"
    )

    /// en = "A small percentage of individuals may experience epileptic seizures when exposed to colored lights. Use caution if you, or anyone in your family, have an epileptic condition."
    static let get_ready_photosensitivity_dialog_description = LocalizedStringKey(
        "amplify_ui_liveness_get_ready_photosensitivity_dialog_description"
    )

    /// en = "Follow the instructions to complete the check:"
    static let get_ready_steps_title = LocalizedStringKey(
        "amplify_ui_liveness_get_ready_steps_title"
    )

    /// en = "Make sure your face is not covered with sunglasses or a mask."
    static let get_ready_face_not_covered = LocalizedStringKey(
        "amplify_ui_liveness_get_ready_face_not_covered"
    )

    /// en = "Move to a well-lit place that is not in direct sunlight."
    static let get_ready_lighting = LocalizedStringKey(
        "amplify_ui_liveness_get_ready_lighting"
    )

    /// en = "When an oval appears, fill the oval with your face in it."
    static let get_ready_fit_face = LocalizedStringKey(
        "amplify_ui_liveness_get_ready_fit_face"
    )

    /// en = "Begin Check"
    static let get_ready_begin_check = LocalizedStringKey(
        "amplify_ui_liveness_get_ready_begin_check"
    )

    /// en = "Illustration demonstrating good fit of face in oval."
    static let get_ready_illustration_good_fit_a11y = LocalizedStringKey(
        "amplify_ui_liveness_get_ready_illustration_good_fit_a11y"
    )

    /// en = "Illustration demonstrating face too far from screen."
    static let get_ready_illustration_too_far_a11y = LocalizedStringKey(
        "amplify_ui_liveness_get_ready_illustration_too_far_a11y"
    )

    /// en = "REC"
    static let challenge_recording_indicator_label = LocalizedStringKey(
        "amplify_ui_liveness_challenge_recording_indicator_label"
    )

    /// en = "Hold face in oval for colored lights."
    static let challenge_instruction_hold_face_during_freshness = LocalizedStringKey(
        "amplify_ui_liveness_challenge_instruction_hold_face_during_freshness"
    )

    /// en = "Move back"
    static let challenge_instruction_move_face_back = LocalizedStringKey(
        "amplify_ui_liveness_challenge_instruction_move_face_back"
    )

    /// en = "Move closer"
    static let challenge_instruction_move_face_closer = LocalizedStringKey(
        "amplify_ui_liveness_challenge_instruction_move_face_closer"
    )

    /// en = "Move closer"
    static let challenge_instruction_move_face = LocalizedStringKey(
        "amplify_ui_liveness_challenge_instruction_move_face"
    )

    /// en = "Hold still"
    static let challenge_instruction_hold_still = LocalizedStringKey(
        "amplify_ui_liveness_challenge_instruction_hold_still"
    )

    /// en = "Ensure only one face is in front of camera"
    static let challenge_instruction_multiple_faces_detected = LocalizedStringKey(
        "amplify_ui_liveness_challenge_instruction_multiple_faces_detected"
    )

    /// en = "Connecting..."
    static let challenge_connecting = LocalizedStringKey(
        "amplify_ui_liveness_challenge_connecting"
    )

    /// en = "Verifying"
    static let challenge_verifying = LocalizedStringKey(
        "amplify_ui_liveness_challenge_verifying"
    )

    /// en = "Cancel Challenge"
    static let challenge_cancel_a11y = LocalizedStringKey(
        "amplify_ui_liveness_challenge_cancel_a11y"
    )

    /// en = "Change Your Camera Settings"
    static let camera_setting_alert_title = LocalizedStringKey(
        "amplify_ui_liveness_camera_setting_alert_title"
    )

    /// en = "Allow camera permission in settings."
    static let camera_setting_alert_message = LocalizedStringKey(
        "amplify_ui_liveness_camera_setting_alert_message"
    )

    /// en = "Update Setting"
    static let camera_setting_alert_update_setting_button_text = LocalizedStringKey(
        "amplify_ui_liveness_camera_setting_alert_update_setting_button_text"
    )

    /// en = "Not Now"
    static let camera_setting_alert_not_now_button_text = LocalizedStringKey(
        "amplify_ui_liveness_camera_setting_alert_not_now_button_text"
    )

    /// en = "Close"
    static let close_button_a11y = LocalizedStringKey(
        "amplify_ui_liveness_close_button_a11y"
    )

    /// en = "Good fit"
    static let get_ready_good_fit_example = LocalizedStringKey(
        "amplify_ui_liveness_get_ready_good_fit_example"
    )
    
    /// en = "Too far"
    static let get_ready_too_far_example = LocalizedStringKey(
        "amplify_ui_liveness_get_ready_too_far_example"
    )
    
}
